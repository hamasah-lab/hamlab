import { PropsWithChildren } from 'react'

export const metadata = {
  title: 'Register',
}

const RegisterLayout = (props: PropsWithChildren) => {
  return <>{props.children}</>
}

export default RegisterLayout
