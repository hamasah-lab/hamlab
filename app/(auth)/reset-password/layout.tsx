import { PropsWithChildren } from 'react'

export const metadata = {
  title: 'Reset Password',
}

const ResetPasswordLayout = (props: PropsWithChildren) => {
  return <>{props.children}</>
}

export default ResetPasswordLayout
